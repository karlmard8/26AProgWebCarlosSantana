{
  "dev": "vite --host 0.0.0.0",
  "name": "frontend_tecnico",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/main.ts --outfile=./dist/main.js",
    "dev": "bun run build && bunx serve . -l tcp://0.0.0.0:5171",
    "start": "bun run build && bunx serve . -l tcp://0.0.0.0:5171"
  },
  "devDependencies": {}
}